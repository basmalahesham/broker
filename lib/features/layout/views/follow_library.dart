import 'package:flutter/material.dart';

class FollowLibrary extends StatelessWidget {
  const FollowLibrary({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
